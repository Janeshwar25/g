apiVersion: v1
kind: Service
metadata:
  name: forge-plan-gen
spec:
  ports:
    - name: 8080-tcp
      port: 8080
      protocol: TCP
  selector:
    app: APPLICATION_NAME
  sessionAffinity: None
  type: ClusterIP